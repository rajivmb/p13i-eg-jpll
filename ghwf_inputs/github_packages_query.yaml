query: '
  query($owner:String!, $package:String!, $version:String!) {
     user(login: $owner) {
       login
       id
       packages(first: 1, names: [$package]) {
         nodes {
           version(version: $version) {
             id
           }
         }
       }
     }
   }'
variables:
  owner: rajivmb
  package: com.p13i.mit.aws.example.p13i-java-parent-example
  version: 1.0.0-SNAPSHOT
